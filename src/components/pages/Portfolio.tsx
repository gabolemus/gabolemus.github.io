import Projects from "../molecules/Projects/Projects";
import MainPage from "../templates/MainPage";

/** Component for the page that displays the portfolio */
const Portfolio = () => {
  return (
    <MainPage>
      <Projects />
    </MainPage>
  );
};

export default Portfolio;
